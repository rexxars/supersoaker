'use strict';

var assert = require('assert');
var supersoaker = require('../');

describe('supersoaker', function() {
    it('can do something', function(done) {
        supersoaker();
        assert(false, 'tests not implemented');
        done();
    });
});
